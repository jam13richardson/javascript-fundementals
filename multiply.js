const multiply = (num1, num2) => {
    return num1 * num2;
};

module.exports = multiply;

// in the terminal run;
// const sayHello = require('./sayHello') <---- require the functions 
// sayHello() <---- call the functions 
// then terminal will produce output 